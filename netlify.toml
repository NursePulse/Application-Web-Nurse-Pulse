[build]
  command = "npm run build"
  publish = "dist/FrontNursePulse/browser"

[build.environment]
  NODE_VERSION = "22.12.0"
  NPM_FLAGS = "--include=dev"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
