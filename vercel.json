{
  "buildCommand": "npm run build -- --configuration production",
  "outputDirectory": "dist/FrontNursePulse/browser",
  "rewrites": [
    {
      "source": "/((?!.*\\.).*)",
      "destination": "/index.html"
    }
  ]
}